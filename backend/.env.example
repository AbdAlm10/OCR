# Default model id: trocr-handwritten
OCR_DEFAULT_MODEL=trocr-handwritten

# auto | cuda | cpu | mps
OCR_DEVICE=auto

# Preload default model on API start (false = faster boot)
OCR_PRELOAD=false

OCR_MAX_NEW_TOKENS=64

CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
MAX_UPLOAD_MB=25
